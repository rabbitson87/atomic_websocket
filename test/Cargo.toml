[package]
name = "test"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "1.40.0", default-features = false, features = ["fs"] }
atomic_websocket = { path = "../", features = ["debug"] }

[profile.dev]
opt-level = 0

[profile.release]
opt-level = 3
